diff -Naur httpd-2.4.4-orig/httpd-2.4.4.kdev4 httpd-2.4.4/httpd-2.4.4.kdev4
--- httpd-2.4.4-orig/httpd-2.4.4.kdev4	1970-01-01 01:00:00.000000000 +0100
+++ httpd-2.4.4/httpd-2.4.4.kdev4	2013-07-22 19:06:11.535660276 +0200
@@ -0,0 +1,3 @@
+[Project]
+Manager=KDevCustomMakeManager
+Name=httpd-2.4.4
diff -Naur httpd-2.4.4-orig/modules/dav/fs/dbm.c httpd-2.4.4/modules/dav/fs/dbm.c
--- httpd-2.4.4-orig/modules/dav/fs/dbm.c	2011-12-05 01:08:01.000000000 +0100
+++ httpd-2.4.4/modules/dav/fs/dbm.c	2013-07-23 11:46:56.143874106 +0200
@@ -138,7 +138,10 @@
                 != APR_SUCCESS
         && !ro) {
         /* ### do something with 'status' */
-
+	printf("%s:%d apr_dbm_open error pathname=%s  ro=%d\n",__FUNCTION__, __LINE__, pathname, ro);
+       ap_log_error(APLOG_MARK, APLOG_CRIT, status, ap_server_conf, APLOGNO(00576)
+            "apr_dbm_open error pathname=%s  ro=%d", pathname, ro);
+	
         /* we can't continue if we couldn't open the file
            and we need to write */
         return dav_fs_dbm_error(NULL, p, status);
diff -Naur httpd-2.4.4-orig/server/log.c httpd-2.4.4/server/log.c
--- httpd-2.4.4-orig/server/log.c	2013-01-28 14:13:18.000000000 +0100
+++ httpd-2.4.4/server/log.c	2013-07-30 14:04:29.228359996 +0200
@@ -1433,6 +1433,9 @@
     }
 
     mypid = getpid();
+    
+    printf("ap_log_pid  " __DATE__ "  "  __TIME__ "  pid=%d \n", mypid);
+	
     if (mypid != saved_pid
         && apr_stat(&finfo, fname, APR_FINFO_MTIME, p) == APR_SUCCESS) {
         /* AP_SIG_GRACEFUL and HUP call this on each restart.
@@ -1459,6 +1462,7 @@
                      ap_server_argv0, fname);
         exit(1);
     }
+    printf("ap_log_pid2  " __DATE__ "  "  __TIME__ "  pid=%d  fmt=%s \n", mypid, APR_PID_T_FMT);
     apr_file_printf(pid_file, "%" APR_PID_T_FMT APR_EOL_STR, mypid);
     apr_file_close(pid_file);
     saved_pid = mypid;
diff -Naur httpd-2.4.4-orig/server/main.c httpd-2.4.4/server/main.c
--- httpd-2.4.4-orig/server/main.c	2012-04-29 00:56:20.000000000 +0200
+++ httpd-2.4.4/server/main.c	2013-07-30 12:20:59.393544722 +0200
@@ -14,6 +14,7 @@
  * limitations under the License.
  */
 
+#include <syslog.h>
 #include "apr.h"
 #include "apr_strings.h"
 #include "apr_getopt.h"
@@ -452,10 +453,15 @@
     apr_status_t rv;
     module **mod;
     const char *opt_arg;
+    
+    openlog("apache-dbg", LOG_PID | LOG_NDELAY, LOG_DAEMON);
+    printf("starting Apache debug compiled  " __DATE__ "  "  __TIME__"  pid=%d \n", getpid());
+    syslog(LOG_INFO, "starting Apache debug\n");
     APR_OPTIONAL_FN_TYPE(ap_signal_server) *signal_server;
 
     AP_MONCONTROL(0); /* turn off profiling of startup */
 
+    
     process = init_process(&argc, &argv);
     ap_pglobal = process->pool;
     pconf = process->pconf;
